# Future Improvements

## Mobile view enhancements

On mobile the middle pane (containing weather, calendar, etc) isn't fixed
height, so as it shuffles between views the RSS reader below it moves up and
down.  Should be fixed height so this doesn't happen

As well, only on mobile, that same panel doesn't "animate" between items, so the
transition is jarring.

## NHL Playoff display round

For each NHL game in the NHL panel, display which "round" a game is if it's a
playoff game.  Ex: currently CAR & PHI is round 2, but MTL & TBL is round 1.
